// Renderer process TypeScript code
// This file has access to window.electronAPI (exposed via contextBridge)

console.log('Renderer process initialized');

// Example: You can now use the electron API
// const content = await window.electronAPI.readFile('path/to/file.txt');
// await window.electronAPI.writeFile('path/to/file.txt', 'content');
